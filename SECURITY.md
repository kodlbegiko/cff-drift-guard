# Security policy

This project reads local text metadata only and makes no network calls during checks. Report parser crashes, path-handling issues, or unsafe output behavior through a private GitHub security advisory when available. Do not submit real credentials or private research data in a report.
