# Contributing

Open an issue before changing eligibility rules or version normalization. Changes to study conclusions must include updated machine-readable results, tests, and an explanation of whether the preregistered protocol was altered. Run `python -m pip install -e ".[dev]"`, `ruff check .`, `ruff format --check .`, `mypy src`, and `pytest --cov` before submitting a pull request.
